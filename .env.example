# Smithery MCP 설정
# Visit https://smithery.ai to get your API key and profile
SMITHERY_API_KEY=your_smithery_api_key_here
SMITHERY_PROFILE=your_smithery_profile_here

# Notion API Key (직접 MCP 서버 사용 시에만 필요)
# NOTION_API_KEY=your_notion_api_key_here

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Streamlit configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Optional: MCP Server connection timeout (seconds)
MCP_CONNECTION_TIMEOUT=30

# Optional: Tool execution timeout (seconds)
TOOL_EXECUTION_TIMEOUT=10